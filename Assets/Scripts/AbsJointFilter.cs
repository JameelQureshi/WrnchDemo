using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using wrnchAI.wrAPI;

public class AbsJointFilter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public static Person AdjustJoints(Person person)
    {

        return person;
    }
}
